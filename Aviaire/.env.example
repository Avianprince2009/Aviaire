# Frontend environment (Vite). VITE_ prefix is required for client exposure.
VITE_API_BASE_URL=https://aviaire-backend.onrender.com/api/v1

# Local dev only: optional override when running locally
# VITE_API_BASE_URL=http://localhost:3000/api/v1
